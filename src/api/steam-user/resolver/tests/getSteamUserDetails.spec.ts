import {describe, test, expect} from "vitest";

describe("[resolver: getSteamUserDetails]", () => {
    test('-', () => {
        // Would write more tests here to test this resolver in a similar fashion as the other one.
        expect(true).toBe(true);
    })
});